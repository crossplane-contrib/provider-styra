apiVersion: styra.crossplane.io/v1alpha1
kind: Stack
metadata:
  name: example-stack
spec:
  forProvider:
    readOnly: true
    description: "This is a description"
    type: kubernetes:v2
  providerConfigRef:
    name: styra-provider
