apiVersion: styra.crossplane.io/v1alpha1
kind: Stack
metadata:
  name: example-stack
spec:
  forProvider:
    description: "This is a description"
    type: kubernetes:v2
    readOnly: true
  providerConfigRef:
    name: styra-provider
